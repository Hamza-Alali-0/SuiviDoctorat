Place the production "How It Works" background image here so the start page uses it as a sticky decorative background.

- Target path (recommended): `src/assets/how-bg.png`
- Expected: a wide image (suggested ~1200×800 px) in PNG or JPG format.
- Current fallback: `how-bg.svg` (abstract shapes) is already present and will be used if `how-bg.png` is not found.

To install your image:

1. Save your chosen image file as `how-bg.png`.
2. Copy it into this folder: `frontend-app/src/assets/`.
3. Restart the dev server if it's running (some build setups cache assets).

If you want me to add the exact image you attached in the chat, upload the PNG file into the workspace or tell me where to fetch it and I will add it for you.
